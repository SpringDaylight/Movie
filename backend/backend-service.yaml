apiVersion: v1
kind: Service
metadata:
  name: bolrea-malrea-backend
  namespace: dev
spec:
  selector:
    app: bolrea-malrea-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

